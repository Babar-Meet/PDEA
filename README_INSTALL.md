# YT Local Desktop - Quick Installation Guide

## 🚀 One-Click Installation

### For Windows Users:

1. **Install Dependencies**: Double-click `install.bat`
2. **Run Desktop App**: Double-click `run-electron.bat`
3. **Or Run in Browser**: Double-click `run-browser.bat`

**Note**: If you see permission errors, try right-clicking the .bat file and selecting "Run as administrator".

## 📋 Step-by-Step Instructions

### Prerequisites
- Windows 10/11
- Node.js (https://nodejs.org/) - LTS version recommended

### Installation Process

1. **Extract the Project**: Unzip the downloaded file to a folder on your computer
2. **Install Dependencies**:
   - Right-click `install.bat` → "Run as administrator"
   - Wait for the installation to complete (5-10 minutes)
3. **Launch the Application**:
   - For desktop: Right-click `run-electron.bat` → "Run as administrator"
   - For browser: Right-click `run-browser.bat` → "Run as administrator"

## 🎯 Features

- **Download YouTube videos** in various qualities (up to 4K)
- **Play videos locally** with advanced controls
- **Manage subscriptions** and auto-download new videos
- **Organize videos** into categories and playlists
- **Background downloads** with pause/resume support

## 📂 Files

- `install.bat` - Installs all required dependencies
- `run-electron.bat` - Runs the desktop application
- `run-browser.bat` - Runs the browser version
- `SETUP.md` - Detailed setup instructions and troubleshooting

## ❓ Need Help?

See the full `SETUP.md` file for:
- Detailed troubleshooting
- System requirements
- Manual installation instructions
- FAQ

## 🐛 Issues

If you encounter problems:
1. Ensure you're running the batch files as administrator
2. Re-run `install.bat` to reinstall dependencies
3. Check the troubleshooting section in `SETUP.md`
